import React from 'react';
import BackOfficeUI from './BackOfficeUI'; // Import the UI component

const BackOffice = () => {
  return (
    <div>
      <BackOfficeUI />  {/* Render the UI */}
    </div>
  );
};

export default BackOffice;